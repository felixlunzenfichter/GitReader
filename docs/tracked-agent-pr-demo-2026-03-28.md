# Tracked Agent PR Demo

This PR demonstrates tracked active-agent metadata display in GitReader.

The goal is to surface real-time sub-agent activity (spawns, completions, tool calls)
directly in the git-diff rendering pipeline, so the iPad and Spectacles views
can show what Claude Code agents are doing as they work.

Date: 2026-03-28
